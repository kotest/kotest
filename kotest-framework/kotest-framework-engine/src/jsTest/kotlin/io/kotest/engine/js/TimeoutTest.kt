package io.kotest.engine.js

import io.kotest.core.spec.style.FunSpec

class TimeoutTest : FunSpec() {
   init {
      test("JS engine should set timeout on the underlying promise") {

      }
   }
}
