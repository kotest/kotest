package io.kotest.core.test

// todo restore adding tags to names
//return if (configuration.testNameAppendTags) {
//   TestCase.appendTagsInDisplayName(testCase)
//} else {
//   testCase
//}

// todo restore unique test name check
//val uniqueName = Identifiers.uniqueTestName(
//   testCase.descriptor.name.name,
//   rootTestCases.map { it.descriptor.name.name }.toSet()
//)
//       val description = testCase.descriptor.copy(name = createTestName(uniqueName))
